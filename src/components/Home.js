import SideBar from "./SideBar";
import Header from "./Header";

const Home = () => {
  return (
    <div className='flex w-full '>
      <SideBar />
      <Header/>
    </div>
  );
};

export default Home;
